{
  "private": true,
  "name": "gatsby-theme",
  "version": "1.0.0",
  "scripts": {
    "start": "rm -rf site/.cache && yarn workspace site start"
  },
  "workspaces": [
    "site",
    "packages/*"
  ]
}
